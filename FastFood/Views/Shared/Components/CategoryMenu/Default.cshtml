@*<li class="dropdown">
    <a asp-controller="FastFood"
       asp-action="List"
       class="dropdown-toggle" data-toggle="dropdown">Foods<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="FastFood" asp-action="List"
                   asp-route-categoryid="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="FastFood" asp-action="List" asp-route-categoryid="">View all Foods</a>
        </li>
    </ul>
</li>*@

<li class="nav-item dropdown">
                    <a asp-controller="FastFood"
       asp-action="List" class="nav-link dropdown-toggle"  id="navbardrop" data-toggle="dropdown">
                       Categories
                    </a>
                    <div class="dropdown-menu">
                        @foreach (var category in Model)
                         {
                        <a class="dropdown-item" asp-controller="FastFood" asp-action="List"
                           asp-route-categoryid="@category.CategoryName">@category.CategoryName</a>
                    
                         }
                        <a class="dropdown-item" asp-controller="FastFood" asp-action="List" asp-route-categoryid="">View all Foods</a>
                    </div>
                </li>